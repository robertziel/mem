### SSG (Static Site Generation)

SSG pre-renders pages at build time to static HTML.

- **Key point** -> Great for speed and caching.
- **Key point** -> Content changes require rebuild/deploy.
- **Gotcha** -> Not ideal for highly dynamic data.

Example:
```text
Build -> HTML files -> CDN
```
