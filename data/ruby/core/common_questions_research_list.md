### Common Ruby questions (quick research list)

1. **Symbols vs. strings** — When to use each, mutability, memory use. See `symbols_vs_strings_ruby.md`.
2. **`include` vs. `extend`** — How modules add instance vs. class methods. See `include_vs_extend_ruby.md`.
3. **`attr_reader`/`attr_writer`/`attr_accessor`** — How to generate getters/setters. See `attr_accessor_ruby.md`.
4. **`each` vs. `map`** — Iteration with side effects vs. transformation. See `each_vs_map_ruby.md`.
5. **`require` vs. `load` vs. `require_relative`** — How Ruby loads code. See `require_load_require_relative_ruby.md`.
6. **Class vs. module** — When to use inheritance vs. mixins. See `class_vs_module_ruby.md`.
7. **Instance vs. class variables** — Scope and behavior. See `instance_vs_class_variables_ruby.md`.

**Tip:** This list focuses on topics that frequently show up in Ruby interviews, onboarding docs, and Stack Overflow questions.
