### Bundler

A bundler combines modules into optimized bundles for the browser.

- **Key point** -> Resolves imports and outputs deployable files.
- **Key point** -> Supports code splitting and tree shaking.
- **Gotcha** -> Misconfigured bundlers can produce huge bundles.

Example:
```text
entry.js -> bundle.js
```
