### Core Web Vitals

CWV measures real user experience: LCP, INP, and CLS.

- **Key point** -> LCP: largest element render time.
- **Key point** -> INP: interaction responsiveness.
- **Gotcha** -> CLS: layout shifts from late content.

Example:
```text
LCP < 2.5s, INP < 200ms, CLS < 0.1
```
